# The models in this repository are neural network model and linear regression. 
# But, in the practical1 repository more than 10 models, that I tested and most of them perform well for this experiment but I chose only these two models.
